<%= render "navbar" %>

<div class="quote">
  <blockquote class="pull-right">
    <p>Quality is not an act, it is a habit.</p>
    <small>by <cite title="Source Title">Aristotle</cite></small>
  </blockquote>
</div>

<div class="middle">
  <div class="container">
    <hr />
    <div class="row">
      <div class="span7">
        <!-- Button to trigger modal -->
        <a href="#myModal" role="button" class="btn btn-inverse btn-large" data-toggle="modal" style="position: fixed; top: 100px; right: 82px;">View flowchart</a>
        <% @employee.descriptions.each do |yo| %>
          <%= simple_format yo.job_description %>
        <% end %>
        <hr />
      </div>
      <div class="span5">
        <h4>Latest comments</h4>
          <% if @employee.comments.present? %>
            <% @latest_comments.each do |comment| %>
                <strong><%= comment.commenter %> said at <%= comment.created_at.strftime("%d %B %Y") unless comment.created_at.nil? %>:</strong>
                <em><%= link_to truncate(comment.body, :length => 117), communication_employee_path(@employee) %></em>
                <hr />
            <% end %>
          <% end %>
      </div>
    </div>
  </div>
</div>

<!-- Modal -->
<div id="myModal" class="modal modal-large hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel">Decision making flowchart</h3>
  </div>
  <div class="modal-body">
    <p><%= image_tag("Flowchart.png") %></p>
  </div>
</div>
